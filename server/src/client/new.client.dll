using System.Threading.Tasks;

public class Startup {
    public async Task<object> Invoke(dynamic input) {
        string name = (string)input.name;
        string vat = (string)input.vat;
		return ".NET Welcomes " + name + ' ' + vat;
        return null;
    }
}
