using System.Threading.Tasks;

public class Client {
    public string name = "foo";
    public string vat = "123456";
}

public class Startup
{
    public async Task<object> Invoke(dynamic input)
    {
        Client client = new Client();
        return client;
    }
}
